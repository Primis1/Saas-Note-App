type NoteElement = {
    id: string;
    title: string;
    text: string;
    date: Date;
}

type CreateNoteElement = {
    title: string;
    text: string;
    date: Date;
}

type DeleteNoteElement = {
    id: string
}

